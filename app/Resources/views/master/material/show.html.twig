{% extends 'master/layout.html.twig' %}

{% block title 'Show Material' %}

{% block actions %}
    {{ include('master/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ material.code|default }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ material.name|default }}</td>
            </tr>
            <tr>
                <th>Weighted Purchase Price</th>
                <td>{{ material.weightedPurchasePrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Selling Price</th>
                <td>{{ material.sellingPrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ material.isActive|default ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
