{% set grid_layout = 'datagrid.html.twig' %}

{{ grid_top(grid) }}

<table data-role="table" class="table table-bordered table-striped table-condensed table-hover">
    <thead>
        <tr data-action="sort" class="alert alert-info">
            <th data-group="sampleMaterialOutgoingDetail" data-field="quantity">Quantity</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for sampleMaterialOutgoingDetail in grid.data %}
            <tr data-field-id="{{ sampleMaterialOutgoingDetail.id }}">
                <td class="text-left">{{ sampleMaterialOutgoingDetail.quantity|default }}</td>
                <td class="text-left">
                    {{ include('transaction/actions.html.twig', {
                        'contents': {
                            '{index}': null,
                            '{new}': null,
                            '{show}': include('action.html.twig', {'url': path('transaction_sample_material_outgoing_detail_show', {'id': sampleMaterialOutgoingDetail.id}), 'button': 'btn btn-info btn-sm', 'icon': 'glyphicon glyphicon-info-sign'}),
                            '{edit}': include('action.html.twig', {'url': path('transaction_sample_material_outgoing_detail_edit', {'id': sampleMaterialOutgoingDetail.id}), 'button': 'btn btn-warning btn-sm', 'icon': 'glyphicon glyphicon-edit'}),
                            '{delete}': include('action.html.twig', {'url': path('transaction_sample_material_outgoing_detail_delete', {'id': sampleMaterialOutgoingDetail.id}), 'button': 'btn btn-danger btn-sm', 'icon': 'glyphicon glyphicon-remove'}),
                        }
                    }) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2" class="alert alert-warning">
                    <strong>There are no items to display.</strong>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ grid_bottom(grid) }}
