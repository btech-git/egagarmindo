{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Estimation Detail' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Quantity</th>
                <td>{{ estimationDetail.quantity|default }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ estimationDetail.unitPrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ estimationDetail.total|default|number_format(2) }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
