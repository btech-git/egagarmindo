{% extends 'transaction/layout.html.twig' %}

{% block title 'Show Sample Request Detail' %}

{% block actions %}
    {{ include('transaction/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Type Name</th>
                <td>{{ sampleRequestDetail.typeName|default }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ sampleRequestDetail.quantity|default }}</td>
            </tr>
            <tr>
                <th>Unit Price</th>
                <td>{{ sampleRequestDetail.unitPrice|default|number_format(2) }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ sampleRequestDetail.total|default|number_format(2) }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
